---
import { Image } from "astro:assets"

type Props = {
  src: ImageMetadata
  alt: string
}

const { src, alt } = Astro.props
---

<Image class="m-auto shadow-xl group-odd:order-1" src={src} alt={alt} />
