---
import { Picture } from "astro:assets"

type Props = {
  src: ImageMetadata
  alt: string
}

const { src, alt } = Astro.props
---

<div class="shadow-xl group-odd:order-1 m-auto">
  <Picture src={src} alt={alt} />
</div>
